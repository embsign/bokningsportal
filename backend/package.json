{
  "name": "embsign-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev ../frontend --config ../wrangler.toml --port 8787",
    "dev:remote": "wrangler pages dev ../frontend --config ../wrangler.toml --port 8787"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.14.10",
    "miniflare": "^3.20240524.1",
    "typescript": "^5.5.4",
    "wrangler": "^3.66.0"
  }
}
