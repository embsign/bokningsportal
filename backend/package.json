{
  "name": "embsign-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler --cwd .. pages dev frontend --d1 DB=booking-prod --port 8787",
    "dev:remote": "wrangler --cwd .. pages dev frontend --d1 DB=booking-prod --port 8787",
    "generate:sql": "node ../scripts/bundle_sql_for_worker.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.14.10",
    "miniflare": "^3.20240524.1",
    "typescript": "^5.5.4",
    "wrangler": "^3.66.0"
  }
}
